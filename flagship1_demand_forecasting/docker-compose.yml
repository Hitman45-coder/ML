services:
  api:
    build:
      context: .
      network: host
    ports: ["8000:8000"]
    user: "${CONTAINER_USER:-appuser}"
    environment:
      MODEL_PATH: /app/artifacts/model.joblib
    volumes:
      - ./artifacts:/app/artifacts:ro
